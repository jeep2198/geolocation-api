name: CI/CD Pipeline

on:
  pull_request:
  push:
    branches:
      - "**"
      - "!main"

jobs:
  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.11"

  #     - name: Install lint dependencies
  #       run: pip install black flake8 isort

  #     - name: Run linters
  #       run: |
  #         black --check app
  #         isort --check-only app
  #         flake8 app

  test:
    runs-on: ubuntu-latest
    # needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r app/requirements.txt pytest pytest-cov coverage httpx

      - name: Run tests with coverage
        run: pytest --cov=app --cov-report=xml

  build-and-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image (no push, load locally)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          push: false
          load: true   # 👈 necesario para que Trivy la vea en local
          tags: geolocation-api:ci

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@0.20.0
        with:
          image-ref: geolocation-api:ci
          format: table
          exit-code: 1   # Falla si hay CRITICAL/HIGH
          severity: CRITICAL,HIGH


  push:
    runs-on: ubuntu-latest
    needs: build-and-scan
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/geolocation-api:latest
